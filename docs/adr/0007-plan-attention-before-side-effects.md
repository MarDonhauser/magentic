# Plan attention before side effects

Attention policy is derived once from a coherent Observation plus break,
deployment, active-Session, and quiet signals. The stateful planner owns
confirmation, priority, cadence, deduplication, and suppression; platform
notification, Dock, native-attention, and window calls only execute its intents.
Unavailable runtime knowledge never means all Sessions are dead and never
clears a previously exact badge.
